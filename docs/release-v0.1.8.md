Medeo v0.1.8

- 换源、换线路按可靠集名或集号匹配；无法确认时手动选集，避免跳错集或串写进度。
- 下一集根据最新片目判断，正确处理预告、特别篇、缺集、倒序和末集；不同线路不再重复累计集数。
- 首页增加最近观看和完整记录入口；收藏与观看记录分开，历史支持完整搜索和单条删除撤销。
- 详情增加主播放按钮、简介展开、集名搜索、排序和当前集定位。
- 播放错误提供有效的重试/换源操作；搜索显示部分源失败情况并保留成功结果。
- 改善小屏、大字体及选集、导航、进度条的无障碍语义。

安装：Android 7.0 及以上，applicationId 为 `com.untr.medeo`，versionCode 9。沿用 v0.1.7 的正式签名，可覆盖升级；依赖和数据库结构不变。

验证：97 项 JVM 测试通过；本轮功能的 16 项 Android 仪器测试通过；Debug、Release 构建和 Release lint 通过（无错误，仅原有依赖更新提示）。已在官方模拟器验证 v0.1.7 → v0.1.8 覆盖安装，收藏、观看进度所有字段以及原设置保留，免责声明不重复弹出；缓存清理和有/无缓存的离线状态正常。APK Signature Scheme v2 签名验证通过。

验证边界：未进行真机及长时间真实媒体播放测试；完整 TalkBack 听读流程未验证。

APK SHA-256：`1790545a8d93636fd56a5a38fb5e1782e6551ef242d03e207f845921122db58f`
